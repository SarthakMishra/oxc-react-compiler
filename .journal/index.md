# Implementation Journal

> Chronological log of implementation sessions. Each file covers ~50 entries before splitting.

| File             | Entries | Phases | Notes                                                                  |
| ---------------- | ------- | ------ | ---------------------------------------------------------------------- |
| [001.md](001.md) | 77      | 1–78   | Full compiler: HIR foundation, BuildHIR, SSA, optimization, inference, reactive scopes, validation, pipeline, lint rules, testing, end-to-end wiring, structured diagnostic categories, source map exposure, codegen correctness & memoization pipeline foundation, E2E dual-mode rendering tests, sprout eval harness, benchmark fixture pipeline, benchmark harness v2 with timed NAPI, correctness analysis, CI pipeline, error diagnostic full coverage, post-codegen semantic validation, real-world benchmark fixtures, README correctness scoring, Babel differential analysis and snapshots, headless render comparison, P0 critical bug fixes (destructured params + dependency filter), conformance rate 13.4% → 21.8% (TS type stripping, validation bail-out, zero-cache-slot skip, divergence analysis), conformance 81→84 (exhaustive operand walk, ForIn/ForOf codegen, normalization), conformance hardening (import sorting, known-failure triage, divergence analysis — 86/1717, 0 unexpected), clippy hardening + OutputMode::Lint + upstream error matching + hooks-as-values validation (86→255/1717, 5.0% → 14.8%), recursive temp use-counting for cross-scope expression inlining (304/1717, foundational for Gap 2/5/6), JSX syntax preservation in codegen (emit `<div>{count}</div>` not `_jsx(...)`, 23 snapshots updated), sentinel scope infrastructure (is_allocating field + helper, scope creation gated pending P2 validation gaps, 304/1717 unchanged), validate_no_eval pass (EvalUnsupported diagnostic, eval added to is_global_name, wired at pipeline 14.6), sentinel scope emission activated (gate lifted, prune_scopes protected, 35 known-failure regressions filed, 3 newly passing, net 304→272), over-scoped dependency reduction: stable-type firewalls in infer_reactive_places + PropertyLoad/StoreContext fixpoint in propagate_dependencies + useTransition/useOptimistic/useActionState hook typing (272/1717 unchanged, structurally correct prerequisite for Gap 3+4 gains), validate_no_mutation_after_freeze: name-based frozen tracking for JSX props/children, Mutate + MethodCall + PropertyStore mutation detection, DIVERGENCE: name-based vs stable ID tracking (272→278/1717, +6 fixtures), ValidatePreservedManualMemoization: pipeline gate fixes (Pass 5 + Pass 61), scope-spanning bracket validation, upstream error message alignment "This value" (278→309/1717, +31 fixtures), scope pruning correctness: prune_always_invalidating_scopes full implementation (allocating-expression seeding + LoadLocal/StoreLocal propagation) + prune_non_escaping_scopes reassignment escape check + early_return_value guard (309/1717 unchanged, structural correctness), exhaustive dep validation: extra dep detection + ExhaustiveDepsMode gating + all-capture collection with reactive filtering + sorted output + SSA resolution in declared deps (309→315/1717, +6 fixtures), property-path dependency resolution: temp_map for LoadLocal→PropertyLoad chains + collect_read_operand_places_for_deps + dependency_display_name in codegen + sentinel slot fix + TemporaryInfo refactor + DependencyPathEntry PartialEq/Eq (315→318/1717, +3 fixtures), non-reactive propagation for Destructure/CallExpression + name-based dep comparison in merge_scopes via DepKey + double-merge prevention + DependencyPathEntry Ord (318/1717 unchanged, structural correctness), validate_no_mutation_after_freeze extended: hook-return pre-freeze + function-capture freeze + nested function mutation scanning + resolve_name helper (318→331/1717, +13 fixtures), outer-scope reassignment detection: name-based scope analysis in validate_no_global_reassignment + async .then() recursion in validate_locals_not_reassigned + function-declaration DCE fix in build.rs (331→339/1717, +8 fixtures), hooks-in-nested-functions Rule 4 (check_hooks_in_nested_functions + check_nested_hir_for_hook_calls, 339→343/1717, +4 fixtures) + MergeOverlappingReactiveScopes DSU rewrite investigated and reverted (data-flow prerequisite identified), MergeOverlappingReactiveScopesHIR complete 3-phase rewrite: active-scope-stack + DisjointSet<ScopeId> overlap+cross-mutation merge matching upstream algorithm (343→342/1717, −1 from pre-existing indirect prop mutation limitation not merge algorithm), MergeReactiveScopesThatInvalidateTogether safety-check infrastructure: LastUsageMap pre-pass + IntermediateAccumulator + is_simple_instruction + are_lvalues_last_used_by_scope (342/1717 unchanged, pure infrastructure), scope_is_eligible_for_merging predicate: no-reassignments + always-invalidating-type check (342/1717 unchanged, pure predicate; unblocks Sub-task 4b merge loop), MergeReactiveScopesThatInvalidateTogether 3-pass merge loop: identical-deps + output-to-input chaining via can_merge_scopes + merge_scope_deps; all dead_code annotations removed (342/1717 unchanged, compound gains pending 4c/4f), nested scope flattening (Sub-task 4c), transitive dep resolution in propagate_scope_dependencies_hir: StoreLocal declaration tracking + fixpoint decl_deps_map substitution loop matching upstream collectTemporariesSidemap/visitOperand (342/1717 unchanged, structural correctness), Gap 11 fix: prune_non_escaping_scopes in_scope flag removed → collect_used_ids collects all operand uses unconditionally, derived computations now emitted inside scope guards (342/1717 unchanged, structural correctness), LoadGlobal now inserts binding.name into non_reactive_names preventing globals leaking into scope deps via name-based fallback (342/1717 unchanged, Gap 6 structural correctness), sentinel scope slot count off-by-one fix: count_cache_slots + codegen_scope allocation corrected to match upstream CodegenReactFunction.ts N-slot (not 1+N) layout (342→349/1717, +7 fixtures, known-failures.txt −7), free variable detection in propagate_scope_dependencies_hir: locally_defined_names collection + id_to_name map + hook call exclusion from CallExpression non-reactivity rule + collect_destructure_names helper + param_names pipeline param (349→354/1717, +5 fixtures, 20.6%), nested function scanning in validate_no_ref_access_in_render (recursive body walk + ref alias tracking via FxHashSet) + transitive setState detection in validate_no_set_state_in_render (fixpoint chain resolution + StoreLocal lvalue propagation, 354→362/1717, +8 fixtures, 21.1%), pre-freeze component props/hook args in validate_no_mutation_after_freeze: param_names parameter + entry-point frozen_values seeding, DIVERGENCE: approximates upstream InferMutableRanges transitive freeze with explicit param_names (362→368/1717, +6 fixtures), DeclarationId-based dep identity in propagate_dependencies + merge_scopes: TemporaryInfo.root_declaration_id + scope_written_decl_ids + decl_id_consumers + DepKey migrated from string to (Option<DeclarationId>, IdentifierId, path) (368/1717 unchanged, correctness + Sub-task 4f), known-incompatible module detection + ESLint suppression bail-out (file-level, DIVERGENCE from upstream per-component) + useMemo non-array deps rejection + capitalized call alias resolution via StoreLocal/StoreContext propagation (368→374/1717, +6 fixtures, Gap 9), delete expr lowering (PropertyDelete/ComputedDelete HIR nodes) + phi-node freeze propagation + alias freeze tracking + derivation chain tracking (for-of iterator derivation from frozen props) + outer-scope PropertyStore/MethodCall detection + render helper global mutation detection (374→384/1717, +10 fixtures, Gap 9), hook-call capture freeze detection via body-scan approach: collect_inner_declared_names + LoadLocal/LoadContext capture inference replacing context-Vec approach (384→388/1717, +4 fixtures), destructuring assignment lowering (ArrayPattern/ObjectPattern lhs → ComputedLoad/PropertyLoad + StoreLocal/StoreGlobal) + component-level StoreLocal reassignment check in validate_no_global_reassignment (388→391/1717, +3 fixtures), hook alias detection (collect_hook_aliases import scan + EnvironmentConfig.hook_aliases) + dynamic hook identity Rule 5 (StoreLocal chain resolution for non-stable callees) + alias awareness in all 4 existing hook rules (391→416/1717, −31 known failures, 1332→1301), graph-based BFS mutation propagation rewrite in infer_mutation_aliasing_ranges: AliasingGraph + Node + PendingMutation data structures, BFS mutate() with temporal guards, pending_phis back-edge handling, add_edge() helper, creation_map extended for CreateFunction/CreateFrom (370/1717 unchanged), refine_effects() Phase 3.5: Apply resolution with/without signature + CreateFrom/Assign/Capture/MutateConditionally/Mutate value-kind refinement via AbstractHeap.value_kind() (370→385/1717, +15 fixtures, 22.4%), function signature resolution for Apply effects (Iteration 24): analyse_functions rewrite → FxHashMap<IdentifierId, FunctionSignature> with per-parameter effect extraction + fixpoint propagation; refine_effects() Apply resolution via signature lookups; fn_signatures threaded through aliasing_effects + pipeline (385/1717, 22.4%), interleaved fixpoint loop for InferMutationAliasingEffects: AbstractHeap methods return bool for change tracking, merge_value_kinds() lattice join, dead Apply arm removed from process_effect_for_heap (385/1717, 22.4%, structural correctness for multi-pass convergence), arrow function preservation in codegen: is_arrow field on HIRFunction + ReactiveFunction + codegen branch for `() => {}` syntax (385→389/1717, +4 fixtures) + pre_freeze_params infrastructure for component param freezing in aliasing pass (groundwork, no immediate score impact), rename_variables pass: sequential tN naming for reactive scope output declarations + scan_max_temp_counter collision avoidance + can_rename_scope_decl eligibility predicate + count_reads_in_value/terminal exhaustive visitors (397→399/1717, +2 fixtures), infer_reactive_places parameter-only seeding: param_names guard in entry-block DeclareLocal loop matching upstream InferReactivePlaces.ts fn.params seed (399→430/1717, +31 fixtures, 25.0%), validate_no_unsupported_nodes pass + HIR UnsupportedNode detection for object getter/setter, new.target, for-await + JSX prop safety narrowed to onXxx event handlers in validate_no_global_reassignment (430→434/1717, +4 fixtures, 25.3%), setState name heuristic gated behind enableTreatSetIdentifiersAsStateSetters config flag: useState destructure pre-pass for confirmed setState IDs + name-based cross-scope propagation (434→435/1717, +1 fixture), hybrid effects+instruction frozen-mutation validator rewrite: MutateFrozen effects layer + targeted MethodCall/PropertyStore/nested-function instruction checks, hook-return pre-freeze + call-capture freeze, skip conditional mutations for Call/MethodCall Apply fallback (435→424/1717, −11 from false-bail-out exposure; frozen-mutation bail-outs 158→89), stable IdentifierId refactor: ssa_version field on Identifier + binding_ids registry in HIRBuilder so all references to the same binding share one IdentifierId, SSA pass rewritten to use version stacks of u32, eliminate_redundant_phi migrated to SsaId=(IdentifierId,u32) tuples, 15 construction sites updated across 9 files (all 160 tests pass, 1293 known failures unchanged — foundational correctness change) |

## Archive

| File                           | Notes                              |
| ------------------------------ | ---------------------------------- |
| [2026-03-11.md](2026-03-11.md) | Initial project planning session   |
