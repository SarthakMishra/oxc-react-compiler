# Implementation Journal

> Chronological log of implementation sessions. Each file covers ~50 entries before splitting.

| File             | Entries | Phases | Notes                                                                  |
| ---------------- | ------- | ------ | ---------------------------------------------------------------------- |
| [001.md](001.md) | 176     | 1–176  | Full compiler: HIR foundation → 32% conformance (555/1717). Recent: Phase 175 — Stage 3 scope inference divergence deep-work (555/1717, +0, no code changes): mutation aliasing pipeline confirmed structurally correct (all 3 stages match upstream architecture); stale DIVERGENCE comment at infer_mutation_aliasing_ranges.rs:507 identified (Apply skip IS correct); 4 divergences in infer_reactive_scope_variables ranked: effective_range workaround (Rank 1, causes -40 when removed), missing start>0 guard (Rank 2, tested -2 net), any_reactive&&any_mutable pre-filter (Rank 3), missing allocating cases (Rank 4, tested +1/-1); all fixes individually correct but interact with effective_range workaround — correct fix sequence: (1) fix range accuracy, (2) enable use_mutable_range, (3) apply scope inference fixes. Phase 174 — validate_no_unsupported_nodes + validate_no_mutation_after_freeze (+2, 553→555): rewrote check_update_context_identifiers (name-based outer/inner scope matching), added Check 6 frozen-context reassignment detection with frozen_at ordering guard. Phase 173 — effective_range investigation (553/1717, +0, all code reverted): 3 approaches tested against the effective_range / last_use workaround — StoreLocal range extension (-10 slot accuracy), Apply effect handling (-10 slot accuracy), use_mutable_range=true retest (-40 conformance) — all reverted; balance problem confirmed: widening and narrowing both cause regressions; correct fix requires upstream's exact Apply resolution / SignatureModel logic, not local approximations; AliasingEffect::Apply completely skipped in our range inference. Phase 172 — first successful scope inference fix (+3, 550→553): excluded CallExpression/MethodCall from is_mutable_instruction (only literal allocations trigger scope creation), MutateConditionally for unknown MethodCall receiver; 3 newly passing fixtures (rules-of-hooks-23dc7fffde57, template-literal, unknown-hooks-do-not-assert); last_use gate removal (-5) and use_mutable_range=true (-40) both reverted. Phase 171 — scope inference investigation (550/1717, +0, all code reverted): 4 approaches tried across infer_reactive_scope_variables, propagate_dependencies, and validate_preserved_manual_memoization — all blocked by scope surplus; 52 error fixtures have scopes our system creates that upstream doesn't; 21 total approaches across 4 sessions confirm scope surplus is the single gating bottleneck; Stage 3 prerequisites (mutable range accuracy, receiver mutation effects, dep.reactive audit) are the correct next steps. Phase 170 — Check 1 investigation (550/1717, +0, all code reverted): all 194 Check 1 bails have `decl_scope_id = None` — scope inference never assigns a scope to FinishMemoize decl targets; 3 approaches tried (pruned-scope tracking, None→false semantics, root cause analysis), 56 bails are load-bearing via accidental None→true correctness; fix requires Stage 3b scope inference work. Phase 169 — unnamed dep skip in propagate_scope_dependencies_hir Phase 2 else-branch eliminates all Check 2 bails (154→0), Check 1 bails rise to 194, slot accuracy +3 (238 vs 235); blocker shifts from dep-resolution to scope-inference (Stage 3b). Phase 168 — Stage 4f investigation, all remaining tractable items blocked (550/1717, +0, all code reverted): 7 "Found 1 error:" fixtures all blocked (JSX context-variable detection, scope dep resolution, new-mutability infrastructure), 9 silent bail-outs are scope inference gaps not validation gaps, 3 implementation approaches (destructure target extraction, StoreLocal guard removal, enhanced temp_map) all zero-impact or harmful — enhanced temp_map resolution confirmed COUNTERPRODUCTIVE (resolves tN deps to wrong named values, creates more mismatches); fix must change dep-collection in propagate_scope_dependencies_hir not downstream resolution; Phase 167 — tN dep resolution deep investigation (550/1717, +0, all code reverted): StoreLocal is #1 unresolvable-dep producer (43%), defining_operands backward-trace approach prototyped but semantically insufficient — traced root is computation input not user's dep, fix must happen at dep-collection time in propagate_scope_dependencies_hir; Phase 166 — scope completion tracking in validate_preserved_manual_memoization (+1, 549→550); Phase 165 — ObjectExpression computed key bail-out (+4, 536→540); Phase 164 — try-catch value block bail-out (+10, 526→536); Phase 163 — validatePreserveExistingMemoizationGuarantees default enabled (+30, 496→526, temp-name skip attempted/reverted); Phase 162 — Infer-mode body_has_hooks_or_jsx shallow AST walk (+4, 492→496), reconciled known-failures.txt (removed 24, added 38); Phase 161 — Stage 3b deep-work session, all 3 approaches net-negative (operand liveness -24/-17, 0-slot codegen -51, prune_non_reactive_deps -107), all reverted; root cause: mutable_range narrower than upstream — blocked until infer_mutation_aliasing_ranges adds receiver mutation effects and reverse scope propagation; Phase 160 — gating directive stripping in codegen (+2, 505→507), hook-as-value false positive fix in validate_hooks_usage (correctness, 0 net), per-fixture bail-out diagnostics; Stage 3b scope inference investigation — failed is_allocating_instruction fix (-5 net, reverted), check_nested_method_call_as_argument removal (-2 net, reverted), silent bail-out root cause confirmed, SLOTS-MATCH systemic analysis (505→505, investigation only); Stage 2g error.* bail-out sweep — fbt duplicate tags, ref-to-function, self-referencing const, dynamic gating invalid identifier (+6, 499→505); Stage 4e error.* fixtures — setState-in-useMemo indirect detection, nested MethodCall-as-argument check, nested destructuring assignment bail (+4, 495→499); Stage 2d frozen-mutation false-positive investigation — 3 approaches tried, all no-op or reverted, root cause in aliasing pass transitive freeze propagation (MaybeFrozen via PropertyStore/Capture), IIFE detection improvement kept, 495→495 blocker; Stage 4b preserve-memo dep validation fix — remove is_temp_name skip, fix compare_deps Subpath return, pre-compute HIR temp map before build_reactive_function (+31, 464→495); Stage 3a2 zero-slot surplus investigation — 3 failed approaches (per-function guard -44, prune_unused_scopes guard +3 but unresolved-ref bug, prune_non_escaping audit) — root cause confirmed as Stage 3b liveness range issue in infer_reactive_scope_variables; snapshot fixes for 2 pre-existing test failures; Stage 5b dead branch elimination in CP pass — internal fixpoint loop, try_fold_terminal for If/Branch/Ternary/Optional, full post-fold cleanup (unreachable block removal, predecessor rebuild, phi pruning, redundant phi elimination, block merging); 464→464 infrastructure, Stage 5a enhanced DCE (StoreLocal/PrefixUpdate/PostfixUpdate removal, phi-node CP, iterative CP+DCE loop post-validators, +7, 457→464), Stage 3a2 test-position escape detection in prune_non_escaping_scopes — set-based analysis with alias chain propagation (+1, 456→457), Stage 4b validateInferredDep Check 2 — ManualMemoDependency types, two-pass validation architecture, compare_deps, is_temp_name filter (+3, 453→456), Freeze propagation through destructuring and effect callback Check 4b (+1, 452→453), Stage 2b bail-outs — known-incompatible import re-enable, eslint-suppression rule bail, object key quoting, declare-merge optimization (+5, 447→452), Stage 1d/2b codegen polish — dynamic gating directive parsing, empty catch codegen, computed key bail-out removal, const/let/var keyword selection (+6, 441→447), Stage 4e-D for-loop-in-try detection (Terminal::For structured lowering) and file-level bail propagation fix (ANY_FUNCTION_BAILED thread-local, +3), Stage 3b scope inference investigation (no code changes — attempted is_allocating_instruction fix caused -5 net, reverted; root cause is union-find over-merging via last_use_map), Stage 1d Phase 1 lazy scope declaration placement (+6), Fix validate_no_ref_access_in_render post-inline_load_local_temps (+1), Stage 3a comprehensive slot diff and failure categorization (investigation only), Stage 4e-B hooks-in-loop detection via Terminal::Branch (+1), Stage 4e-A upstream error fixture bail-outs (+7), Stage 4d frozen-mutation false negative fixes (+9), Stage 4c Todo error detection (+15), Stage 2a/2b/2c bail-out fixes, Stage 1c codegen fixes, mutation range propagation fixes (+93 fixtures) |

## Archive

| File                           | Notes                              |
| ------------------------------ | ---------------------------------- |
| [2026-03-11.md](2026-03-11.md) | Initial project planning session   |
